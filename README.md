# Homebrew tap for ipcheck

Install [ipcheck](https://github.com/jacklv-coder/ipcheck):

```bash
brew tap jacklv-coder/tap
brew install ipcheck
```

Upgrade later with:

```bash
brew update
brew upgrade ipcheck
```
